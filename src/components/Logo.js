import logo from "../img/logo-white.png";
import React from "react";

const Logo = () => {
  return (
    <div class="logo-box">
      <img src={logo} alt="Logo" class="logo" />
    </div>
  );
};

export default Logo;
